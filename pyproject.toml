[project]
name = "space_cloud"
version = "0.1.1"
description = "ZatGo Space Cloud — hosting vertical on Space control-plane framework"
authors = [{ name = "ZatGo Innovation" }]
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["space_cloud*"]
